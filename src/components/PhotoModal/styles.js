import { css } from 'emotion';

export const closeBtn = css`
  background: transparent;
  border: 0;
  line-height: 1;
  padding: 0;
  top: 5px;

  &:hover,
  &:focus,
  &:active {
    color: #fff;
    opacity: 0.8;
  }
`;
